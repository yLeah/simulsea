import 'package:flutter/material.dart';

class DC extends StatelessWidget {
  const DC({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Washington, D.C.'),
        centerTitle: true,
      ),
      body: Center(
        child: Column(
          children: const [],
        ),
      ),
    );
  }
}
